# Data

Place raw datasets in `data/raw/` and generated artifacts in `data/processed/`.

Expected raw file:

- `kc_house_data.csv`

A copy of `kc_house_data.csv` already exists in the repository root. New reproducible runs should prefer `data/raw/kc_house_data.csv` and keep generated outputs out of Git.
